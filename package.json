{
  "name": "durable-agent-harness",
  "private": true,
  "version": "1.0.0",
  "description": "Public notebook on building a durable AI agent harness",
  "scripts": {
    "sync": "bash scripts/sync-content.sh",
    "cards": "python3 scripts/render_note_social_cards.py",
    "privacy": "bash scripts/privacy-check.sh",
    "serve": "npx --yes serve docs -l 4173",
    "test:e2e": "npx playwright test",
    "test": "npm run sync && npm run privacy && npm run test:e2e",
    "check": "npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
